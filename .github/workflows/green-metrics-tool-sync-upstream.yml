name: Sync Fork with Upstream
on:
  schedule:
    - cron: "30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 50

      - name: Set up Git user
        run: |
          git config --global user.name "Arne Tarara"
          git config --global user.email "arne@green-coding.io"

      - name: Add remote repository
        run: |
          git remote add upstream https://github.com/nextcloud/server
          git fetch --depth 50 --no-recurse-submodules --no-tags upstream master

      - name: Check for updates
        run: |
          CHANGES=$(git rev-list HEAD..upstream/master --count)
          if [ "$CHANGES" -gt 0 ]; then
            echo "Updating fork master branch..."
            git checkout master
            git merge upstream/master
            git push
          else
            echo "No updates from the original repository."
          fi
      - name: Merge in new changes from GMT branch
        run: |
            git checkout green-metrics-tool
            git pull
            git checkout master
            git merge green-metrics-tool
            git push
